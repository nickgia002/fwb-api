export enum USER_ROLE {
  FW = 1,
  SUBCONTRACTOR=2,
  STOPPINGPOINT=3,
}

export enum FLAG_VALUE {
  OFF = 0,
  ON = 1,
}

export const DEFAULT_LIMIT_FOR_PAGINATION = 10;
export const ORDER_LIMIT_FOR_PAGENATION = 20;
export const DEFAULT_ORDER_DIRECTION_DESC = 'desc';
export const DEFAULT_ORDER_DIRECTION_ASC = 'asc';
export const DEFAULT_OFFSET = 0;

export const DEFAULT_FORMAT_DATE = 'yyyy-MM-dd';
